const { add, sum } = require('./utils/math')

const message = 'Hello world'

console.log('message', message)

console.log(address)

const foo = () => {
    console.log('foo funciton exec.')
}
foo()

add(10, 20)
sum(30, 30)